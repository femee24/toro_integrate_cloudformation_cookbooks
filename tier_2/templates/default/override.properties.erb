##this file should only contain properties that will OVERRIDE the default properties file.
#if the values here are the same as the main toro-integrate.properties file, delete them from here

http.port=<%= node[:application][:http_port] %>
#https.port=<%= node[:application][:https_port] %>
http.proxy.port=<%= node[:application][:http_proxy_port] %>
#https.proxy.port=<%= node[:application][:https_proxy_port] %>
#https.keystoreFile=<%= node[:application][:keystore_file] %>
#https.keystorePass=<%= node[:application][:keystore_pass] %>
jms.file=<%= node[:activemq][:jms_file] %>
jms.url=<%= node[:activemq][:jms_url] %>
solr.mode=<%= node[:solr][:mode] %>
solr.prefix=<%= node[:application][:code] %>
solr.url=<%= node[:zookeeper][:cluster] %>
